export class Dealer {
   
    constructor(public slno: string, public companyname: string, public dealername: string, public district: string, public address: string, public email: string, public mobile: string, public pan: string, public gst: string)
     {}
  
}