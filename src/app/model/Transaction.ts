export class Transaction {
   
    constructor(public transactionid: string, public dates: string, public credit: string, public debit: string, public outstanding: string, public description: string, public companyname: string)
     {}
  
}