export class Product {
   
    constructor(public slno: string, public dates: string, public productname: string, public quantity: string, public productslnofromto: string, public companyname: string)
     {}
  
}